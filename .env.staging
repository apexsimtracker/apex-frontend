# Staging build defaults (no secrets). Used by: pnpm build:staging
# Vercel staging branch should also set VITE_API_URL in the dashboard (authoritative).

VITE_APP_ENV=staging
VITE_API_URL=https://staging-y01y.onrender.com
