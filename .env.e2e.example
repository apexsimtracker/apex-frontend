# Loaded by playwright.config.ts (copy to .env.e2e.local — gitignored via *.local)
E2E_BASE_URL=http://localhost:8080
E2E_API_URL=http://127.0.0.1:10000

# Test accounts (sandbox)
E2E_CHECKOUT_USER_EMAIL=iamrohanilyas863@gmail.com
E2E_PRO_USER_EMAIL=hello.worlda1220@gmail.com
# Optional: verified user with no Pro sub for EXPIRATION revoke test (defaults to checkout email)
# E2E_WEBHOOK_USER_EMAIL=
E2E_USER_PASSWORD=

# Optional: override if not present in ../apex/.env
REVENUECAT_WEBHOOK_SECRET=
ADMIN_SECRET=
